

// const redux = redux.createRedux(reducers);
// redux.dispatch(loadLists());
// debugger
class Main extends React.Component {
  render() {
    return (
      <div>
        <Header />
        <Item />
      </div>
    )
  }
};